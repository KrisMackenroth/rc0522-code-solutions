/* exported setValue */
// Using bracket notation, assigned the value
// to the object at the given key
function setValue(object, key, value) {

  object[key] = value;
}
// Using bracket notation, assigned the value
// to the object at the given key
