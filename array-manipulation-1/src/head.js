/* exported head */
// create a new variable for the first index of the array to be pushed to
// iterate through each index of the array
// push the first index of the array into the new variable
function head(array) {
  var firstIndex;
  firstIndex = array[0];

  return firstIndex;
}

//  create a new variable for the first index of the array to be pushed to. Completed
// assign the first index of the array to the new variable. Completed
// return the new variable. Completed
