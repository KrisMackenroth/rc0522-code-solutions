/* exported getFirstChar */
// create a new variable to store the first character of a string.
// assign that new variable to the first index of the string.
// return the new variable.
function getFirstChar(string) {
  var firstChar = '';
  firstChar = string[0];
  return firstChar;
}
// create a new variable to store the first character of a string.
// assign that new variable to the first index of the string.
// return the new variable.
