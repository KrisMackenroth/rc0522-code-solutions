/* exported getLastCharacter */
function getLastCharacter(string) {
  var stringNumber = string.length;
  var lastIndex = stringNumber - 1;
  var lastCharacter = string[lastIndex];
  return lastCharacter;
}
